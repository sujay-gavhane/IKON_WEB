<% collections_json = @collections.map { |product| product.as_json.merge({category_name: product.category.name}) } %>
<% parts_json = @parts.map { |product| product.as_json.merge({category_name: product.category.name}) } %>
<%= react_component("Products", { collections: collections_json, parts: parts_json }) %>
<%= javascript_pack_tag 'products' %>

